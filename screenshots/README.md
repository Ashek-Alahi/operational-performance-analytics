# Dashboard Screenshots

The repository includes a static dashboard preview at `../dashboard/executive_dashboard.html` so the project can be reviewed without Power BI.

If you build a real Power BI `.pbix` version later, export these page screenshots here:

- `executive-overview.png`
- `operations-performance.png`
- `finance-profit-margin.png`
- `accounts-receivable-aging.png`

Do not describe this repository as containing a completed Power BI dashboard until a `.pbix` file or exported Power BI screenshots are added.
